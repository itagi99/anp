{
  "buildCommand": "cd customer-store && npm install && npm run build",
  "outputDirectory": "customer-store/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
